"""内存仓储实现（M2）。

用途（主基线 §24.1 与本项目本机约束）：

- 单元测试注入，保证认证、授权、许可证与审计逻辑在没有 PostgreSQL 的机器上
  也能 100% 覆盖（CI 另有一套针对 PostgreSQL 实现的迁移测试）；
- 本地无数据库演示（``CONTROL_PLANE_REPOSITORY=memory``）。

语义与 PostgreSQL 实现保持一致：唯一约束、状态过滤、只追加审计都在本实现内生效。
对象是按引用保存的（进程内字典），调用方拿到的即存储实例，
与"一次请求一个事务"的 PG 实现差异已知且仅影响开发体验，不影响用例语义。
"""
